// 前端配置文件
const CONFIG = {
    // 后端 API 地址
    API_BASE_URL: 'http://localhost:8080',
    
    // 可用的 agents
    AVAILABLE_AGENTS: ['default', 'assistant', 'coder'],
    
    // 默认 agent
    DEFAULT_AGENT: 'default',
    
    // 终端配置
    TERMINAL: {
        fontSize: 14,
        fontFamily: 'Consolas, "Courier New", monospace, SimHei, WenQuanYi Micro Hei',
        cursorBlink: true
    }
};